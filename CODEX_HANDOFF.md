# StoryCut Studio 开发交接

更新时间：2026-08-31

## 2026-08-31 v0.2.5 深度成片检查（已发布）

第四步手动“成片检查”新增完全本地的 FFmpeg 深度扫描：有当前成片预览时检查连续黑画面、较长静音、平均音量和峰值；尚未生成预览时先扫描真实配音，并明确提示生成预览后再次检查画面。黑场和静音可能是片头片尾的创作留白，因此默认作为 warning，不阻断导出；项目文件缺失、时间线错误和 Shorts 超时仍为 error。深度扫描只在后台手动检查中运行，同步预览前检查不会执行完整解码，避免冻结 QML。配置阈值位于 `quality.*`。使用根 `export/v2-0826_storycut_final_preview.mp4` 实测约 5 秒完成：黑帧和静音通过，音量为平均 -33.8 dB、峰值 -25.8 dB，正确提示整体偏低。

下一项“分层理解长视频”不得替换或默认改变当前已验证的理解与故事生成流程。它应作为独立可选功能，默认关闭；视频达到可配置的时长阈值后，只在第一步给出“建议开启，可改善长视频全局故事理解”的非阻断提示，由用户确认后才启用。普通视频或用户不启用时必须完整沿用当前流程。

## 2026-08-27 v0.2.4 科普术语一致性检查（开发中）

第二步将原科普事实审查与术语一致性合并为一个“文案审查”模块。用户只点击一次，`content_review_service.py` 通过单次 API 请求同时检查事实、原片证据、数字、因果、专业术语、专有名称、缩写、大小写、单位、型号及重复数字；界面只显示一张综合审查卡片和一份建议列表，每条建议用小标签区分“事实与证据”或“术语一致性”。综合结果保存在 `script/content_review.json`，并提供统一术语表、单条应用和批量应用；同一句最多保留一条合并建议，避免相互覆盖。应用建议后会按 GPT-SoVITS 标点规则重新断句和编号，重算预计时长、立即重建参考 SRT；已有镜头匹配时自动重新匹配，因此不需要用户手动移动后续字幕时间。修改故事后两部分报告同时标记 stale，重新生成故事或应用配音超时精简稿时旧报告会被清除。具体 TTS 发音不属于本功能。

## 2026-08-26 理解原片时间显示修正（并入待发布 v0.2.3）

用户在 AMD CPU 笔记本分析 3 分 43 秒视频时，旧逻辑错误显示“预计总用时 35 秒”，随后长期显示“当前阶段超出预估，正在修正”。启动时的历史项目回归/固定公式估算已退出界面，不再显示预计总用时，也不再显示“超出预估”。分析开始后默认显示带三个上下跳动圆点的“正在处理…”，同时保留已用时间和真实进度百分比。

剩余时间只使用当前这一次任务的连续实测：任务至少运行 30 秒，并且最近至少 4 次预测跨越 12 秒、预测完成时刻的波动处于严格容差内，才显示“预计剩余约 …”。进入新阶段或数据重新波动时立即退回“正在处理…”，不会拿前一阶段的估计继续倒计时。当前工作树还包含下述 v0.2.3 真实配音超时智能修正的完整未提交实现，不得覆盖或拆散。

同日修复“最近项目 → 继续”永久停在“加载中…”：无配音项目恢复时，`_update_loaded_voice_status` 错误引用了未定义的 `project_file`，Python 异常会中断 QML Timer，导致加载标志无法复位。现已显式传入项目路径，项目打开入口增加异常兜底，QML 使用 `try/finally` 保证无论打开成功或失败都解除 loading。已用当天实际 `v2-0826` 项目验证，恢复耗时约 0.005 秒并正常载入 54 个事件。

同一实际项目首次生成语音与画面故事时得到预计 210.84 秒的初稿。旧逻辑只会自动重编纯画面故事，语音故事会在最终 179 秒检查处直接失败。现已为语音模式加入最多两次整篇压缩编辑：依据初稿实际估算时长和词数反推安全词数，目标约 174 秒，优先删除重复、次要例子和铺垫，同时强制保留开头价值、关键机制、强证据与完整结尾；不会简单截断末尾。第一次压缩成功即停止，仍超限才进行第二次，因此正常只增加一次最终编辑 API 请求。

组织故事的时长档位默认选中 180 秒，不再默认 60 秒；它是界面上的目标上限，实际生成与超时重编仍按约 174 秒留余量，并以低于 179 秒作为最终硬限制。

2026-08-27 使用为 `v2-0826` 生成的 205 秒测试音频验证配音超时处理。FFmpeg 已成功按 1.15x 生成 178.263 秒工作音频，但完成回调在保存 `audio_size` 时引用了未定义的局部变量 `working_audio`，异常发生在 `_voice_busy = False` 之后、`voiceChanged` 之前，导致界面持续显示“正在处理”。现已在回调内根据当前项目显式解析工作音频路径，并为项目状态保存增加异常提示。实际项目的 `settings.voice` 已恢复为 1.15x、178.263146 秒和正确文件大小，无需重新变速；重启后可直接继续并导入同步 SRT。

同日继续用 229.99 秒音频测试 AI 精简。首版从 308 词过度压缩到 173 词，按实测语速仅约 129 秒。精简服务现升级为 schema 2：根据本次真实语速计算严格的词数上下限，并要求预计成片处于约 160–179 秒；过短和过长都会自动进行最多两次整篇平衡重编。`v2-0826` 的合理目标约为 215–228 词。最终方案会把实测语速按比例写回每句 `estimated_duration_sec`，使用 `measured_voice_projection_v1`，确保弹窗预计、镜头匹配和新 SRT 时间一致；重新打开项目时不得被通用本地估时覆盖。旧 schema 1 方案会自动失效，当前实际项目中的 173 词方案已移除。

AI 精简应用前归档也改为真正可恢复的完整状态：`archive/duration_revision_时间/state/` 保存应用前 `project.json`、整个 `script/`、`timeline/` 和 `audio/`，并写入 `manifest.json`。第四步在应用后显示“恢复精简前版本”，确认后恢复故事、镜头匹配、粗剪、配音、SRT 和项目设置；恢复前的当前版本也会先保存为 `archive/restore_replaced_时间/`，因此可再次撤回。归档提示已改为明确说明完整范围。

恢复按钮不再直接执行恢复，而是先打开双栏对比弹窗：左侧展示当前精简版的词数、预计时长和完整文案，右侧展示归档中准备恢复的超时版，并优先显示其真实配音时长。只有点击右下角“确认恢复右侧版本”才会替换项目；取消会完整保留当前版本。实际 `v2-0826` 项目已验证为当前 225 词、预计 2:48，对比归档 309 词、真实配音 3:50。

第二步新增项目级叙事策略。默认“自动识别（推荐）”，故事模型在原有请求内同时判断科学原理、工程过程、自然生物、人物纪录、宇宙历史或通用故事结构，不额外增加一次 API 请求；用户也可手动指定。选择“不设置（当前逻辑）”时不向现有提示词注入任何叙事结构规则。选择保存在 `project.json -> settings.narrative_strategy`，生成结果同时保存请求策略、实际采用策略和中文理由，旧项目缺少字段时默认自动。

## 2026-08-25 v0.2.3 真实配音超时智能修正

第 4 步在导入 GPT-SoVITS 真实配音后继续以 179 秒为硬安全线。能通过 1.00x～1.25x 整体加速解决时仍走原有本地流程；只有最高安全加速仍不够时，按钮才切换为“AI 精简到 3 分钟”。程序用当前故事词数和这次真实配音时长计算实际语速，以约 176 秒为留余量目标调用最终故事编辑模型，必要时自动二次压缩。

精简结果先保存到 `script/duration_revision_proposal.json`，弹窗并排显示原稿、新稿、词数、真实/预计时长、摘要和主要删改；确认前不替换故事。用户确认后，本地自动重新匹配全部镜头、重建粗剪并生成新的 `script/tts/gpt_sovits_reference.srt`。旧故事、配音、同步 SRT 和 Whisper 结果移入项目 `archive/duration_revision_时间戳/`，不会直接删除。应用后必须重新用 GPT-SoVITS 生成配音；旧精简方案在手动改稿、重新生成故事或重新导入配音时自动失效。

## 2026-08-25 v0.2.2 V1 最终清理与技术画面理解

用户已确认不再保留 V1，历史用户改为重新拉取干净仓库。`storycut_v1/`、`commentary_studio/`、`translator_studio/` 及更新器中的旧目录迁移/删除兼容代码均已移除；根更新清单不再承担旧 V1 清理。`CODEX_HANDOFF.md` 后文仍保留部分 V1 历史记录，仅用于追溯，不代表当前文件结构或兼容承诺。

关键帧视觉理解已加入画面文字、图表、流程图、表格和公式的结构化结果。低清批量请求同时返回 `screen_text` 与 `technical_visual`，不额外增加基础调用；只有重要且不确定的技术画面才从原视频抽取 1440 宽高清帧复查，默认最多 6 个、每批 3 个。高清复查失败不会丢弃第一遍结果。相关配置位于 `vision.technical_visuals_enabled`、`high_detail_review_enabled`、`max_high_detail_events`、`high_detail_batch_size` 和 `high_detail_width`。故事生成和镜头匹配会把这些结果作为证据，同时要求保留单位/符号、不猜模糊文字、不擅自推导公式结论。

第 2 步新增可选科普事实审查，默认 `fact_review.auto_after_story: false`，手动检查或主动打开自动审查时才增加一次 API 调用；模型为空时依次复用最终故事编辑模型和故事模型。审查只发送解说实际引用事件及相邻事件，区分 `source_support`、`general_fact`、`number_unit`、`causality`、`terminology`，保存到 `script/fact_review.json`。结果仅作人工提醒，不自动改稿、不阻断镜头匹配或导出；用户修改解说后报告标记为 stale。该功能没有联网权威来源，界面必须保留免责声明。

事实审查的每个问题要求只绑定一条解说；有明确 `suggestion_en` 时界面显示紧凑的“应用建议”按钮，只替换对应句并立即把报告标记为 stale，之后仍需重新审查。最近项目恢复曾因 `_load_voice` 同步调用 ffprobe 卡住界面约 2～3 秒；现在导入/变速时把 `duration_sec` 和 `audio_size` 缓存在 `settings.voice`，恢复时命中缓存则不调用 ffprobe，旧项目缺少缓存时在后台读取并回写。最近项目按钮会先显示“加载中…”，再延迟一帧开始恢复。

## 2026-08-24 v0.2.0 单应用整合（当前最高优先级状态）

仓库从根版本 `2.0.0` / StoryCut `0.2.0` 起只维护一个 StoryCut 应用。原 V1 中值得保留的能力已经迁入当前 QML 工作流，V1 程序、旧桥接目录和旧启动器已从受跟踪文件中移除。为了兼容既有项目路径，当前应用的内部目录仍叫 `storycut_v2/`；用户启动入口统一为 `点我启动StoryCut（AI解说剪辑）.vbs`。

更新器继续保护 `config.user.yaml`、`projects/`、`output/`、`cache/`、`.env`、`models/`、`venv/` 和 `export/`。历史章节中关于“两套应用”和旧启动器的文字仅用于追溯，不再代表当前结构。

2026-08-25 v0.2.1：仓库更新器显式读取环境变量与 Windows 系统代理，并把代理同时传给 Python HTTP 请求和后台 Git。Raw 版本地址失败时使用 GitHub Contents API，仓库 ZIP 地址失败时使用 `codeload.github.com`，用于改善 Clash 只开启系统代理、未开启 TUN 时的连接与偶发 404。

## 2026-08-24 产品方向与 V1 能力迁移

StoryCut V2 的长期定位是将不限题材的优质长视频（宇宙、星空、生物、机械、科学、人文等）压缩为 3 分钟以内的英文 YouTube Shorts。后续增强按以下原则推进：

- 科普事实审查作为第 2 步内的可选功能，不强制阻断普通项目。
- 分层理解长视频主要服务纯画面或结构复杂素材，当前优先级靠后。
- 画面文字、图表和公式理解优先复用现有关键帧视觉请求，避免为每帧单独增加 API 调用；只有确有价值时再增加局部高清复查。
- 成片质量检查已形成前后两层：导出前检查原片变化、项目产物完整性、179 秒限制、解说覆盖、镜头空隙/重叠/越界/闪切/复用、SRT 顺序/重叠和音画尾差；导出后使用 ffprobe 实测文件、真实时长、分辨率、编码/像素格式和音频轨道。手动检查已放入后台线程并显示 loading，结果以绿色 pass、蓝色 info、黄色 warning、红色 error 卡片呈现。非相邻完全相同镜头仅为正常复用说明；只有相邻区间完全相同才提示先预览是否存在跳回感，warning 不阻断导出。更耗时的黑帧与静音扫描仍留在后续阶段。
- 题材叙事策略不要求用户手动选题。未来采用自动识别题材与叙事目标，并保留“自动”默认项。
- 真实 GPT-SoVITS 配音超时优先在安全范围内加速，再提示删减或重写；绝不超过 179 秒安全线。
- 术语与发音词典分为“文案术语统一”和“TTS 发音提示”两层；前者属于 StoryCut，后者只有 GPT-SoVITS 支持对应输入时才接入。

本轮首先迁移了 V1 已验证的 TTS 整体变速能力：V2 在真实配音超过 179 秒时计算最小适配倍率，1.00x～1.25x 内可一键处理；音频和同步 SRT 使用相同倍率，随后重新校准镜头时间线。导入的原始音频/SRT 分别保存在 `audio/narration_original.wav` 和 `audio/narration_original.srt`，因此可恢复原速且不会累计重复变速。超过 1.25x 仍无法适配时，程序要求删减或重写文案。

V1 字幕高级样式也已迁入 V2 现有 QML 字幕弹窗：新增文字颜色、描边颜色、阴影、斜体、字间距，以及无动画/淡入淡出/轻微弹出。设置仍按项目保存，实际导出由 V2 自己的 ASS 生成器完成，没有重新引入 V1 Tkinter 窗口；旧项目缺少新字段时使用与此前相同的白字、黑边、淡入淡出默认值。

字幕预设现共有六套：标准字幕、清爽描边、Shorts 大字、纪录片暖白、科普黄字、极简白字。预设只是快速填入可编辑参数，不限制用户继续修改颜色、字体、大小、位置和底板。

V1 的同步字幕兜底已迁入 V2：导入英文配音但没有 GPT-SoVITS 同步 SRT 时，可由用户主动点击 Faster-Whisper 英文识别。它复用 V2 已有模型发现、自动下载、CPU/CUDA 选择和后台进度，不默认执行；识别结果保存为工作 `narration.srt`、可恢复的 `narration_original.srt` 和诊断用 `narration_whisper.json`。V2 预览导出也开始在根 `export/_internal/` 持久保存项目名前缀的 FFmpeg 命令日志与滤镜图。

暂存的后续路线：直接内嵌 GPT-SoVITS、批量处理多个视频、自动竖屏裁切和主体跟踪、自动生成标题/简介/标签、根据发布数据优化提示词、素材版权与来源管理、多语言成片。

## 0. 当前权威状态（v0.1.0 初版封版）

StoryCut Studio 的初版完整流程已经实现并由用户实际验证：

1. 理解原片：Faster-Whisper 转写、场景切分、关键帧和事件整理。
2. 组织故事：AI 生成精简英文解说，支持界面编辑和 GPT-SoVITS 文案/SRT。
3. 匹配镜头：候选镜头、替换、边界调整、组合镜头和粗剪时间线。
4. 预览导出：导入 GPT-SoVITS 音频/SRT、移除原声、英文 ASS 字幕及最终视频导出。

当前默认画面策略必须保持：

- 保留源视频真实分辨率和宽高比。
- 不自动 `scale`、`pad` 或 `crop`。
- 不创建竖屏黑色画布，发布平台自行居中显示横版视频。
- 最终音轨只保留英文配音，不保留原声。

当前字幕设计是一套统一的“字幕底板”，同时遮住原字幕并承载英文字幕，不再叠加独立的 ASS 半透明文字背景。底板支持黑色遮罩、局部模糊和 Delogo，区域可拖动并具有方向锁定和吸附；所有数值支持滑块和紧凑精确微调。真实效果通过用户点击“查看真实预览”后调用 FFmpeg 生成，避免实时处理造成卡顿。

用户已经实际测试字幕样式设置与视频导出并确认没有问题。后续开发应视为 v0.1.0 之后的增强，不要回退上述行为。

StoryCut v0.1.1 补充了发布后体验修复：

- “理解原片”前检查根目录 `.env` 的 `OPENAI_API_KEY`。
- 缺少 API 时明确说明本地分析、视觉描述和组织故事之间的区别，并允许用户主动选择仅做本地分析。
- 第 2 步缺少 API 或请求失败时显示持久错误，不再表现为按钮无反应。
- 首页提供 StoryCut 独立版本更新入口，更新范围仅限 `storycut_v2/`。

StoryCut v0.1.2 继续优化首次配置体验：

- 缺少 API 时默认引导“设置 API Key”，不再把本地分析作为主要操作。
- “仅做本地分析”更名为“仅做本地预处理”，明确它只用于提前完成耗时任务，不能替代视觉理解和故事生成。
- 可直接在应用内填写 API Key 和可选接口地址，安全写入根目录 `.env`，保存后立即继续原操作。
- 修复版本更新弹窗更新说明区域的 QML 高度循环警告。

StoryCut v0.1.3 优化四步流程首页、故事生成口吻、GPT-SoVITS 标点断句和 SRT 导出，并将项目恢复入口整理为带确认删除功能的“最近项目”弹窗。

StoryCut v0.1.4 与 Translator v1.0.2 增加启动时静默更新检查和无 CMD 的 VBS 启动入口；旧 BAT 已从仓库删除。历史章节中的 BAT 记录仅作为早期开发背景。

StoryCut v0.1.5 增加缺少 Faster-Whisper 模型时的默认自动下载、独立 GUI 下载进度和下载后自动续跑，并修复超长视频名称撑开首页布局。可通过 `analysis.auto_download_model` 关闭自动下载。

StoryCut v0.1.6 在首页增加常驻 API 设置，可直接管理共享 `.env` 中的 API Key 和根地址；Key 默认遮盖，配置保存后立即生效。视觉描述和故事生成遇到 405 时会提示检查 `/chat/completions` 路由、服务商地址及视觉能力。

v0.1.6 之后的待发布增强：API 设置支持分别选择故事与视觉模型，异步调用中转站 `/models` 并以可搜索弹窗展示，选择结果合并写入 `config.user.yaml`；列表接口不可用时保留手动输入。当前 `ai.comfly.org` 已实测可返回约 850 个模型。

同一待发布版本增加两种内容理解路线：默认“语音与画面”保持 Whisper + 关键帧流程；新增“纯画面叙事”，跳过 Whisper，使用场景关键帧和定时采样帧详细分析人物姿态、动作过程、工具/机器交互、环境变化、可见障碍、结果与连续性，再用专用反臆测提示词组织英文故事。纯画面模式没有有效视觉描述时禁止进入故事生成。镜头匹配继续自动执行，并加入时间顺序与重复使用惩罚，人工候选改为默认隐藏的高级调整。四步卡片可折叠，主页面滚轮距离提高。

针对 319 秒、122 个事件的实测项目，旧提示词在 180 秒设置下只生成 178 词、选择 5 个事件，并因信任模型自报时长错误显示 22.4 秒。现已修复：纯画面模式以约 1.9-2.25 词/秒形成目标词数区间，要求按时长覆盖足够事件和故事阶段，过短/覆盖不足会自动请求一次完整重写；重写后仍严重不足则拒绝结果。程序时长估算改为至少采用实际词数 / 2.25，原178词稿可正确估算约79.8秒。语音模式继续保持“时长为上限、信息不足可缩短”。

同一实测稿中“Next,”、“Finally,”等逗号短语被误计为独立解说单元，造成“26句”观感失真。`split_gpt_sovits_units` 现会把少于3词的碎片合并到相邻单元，避免单词/连接词独占一个 GPT-SoVITS 语音单元。

2026-08-17 纯画面故事生成进一步改为两阶段编辑流程：第一次模型调用只负责通读完整事件时间线，规划人物眼前目标、可见阻碍、尝试、转折、结果和余味，并保存 `script/story_plan.json`；第二次由可单独配置的最终故事编辑模型生成沉浸式第三人称英文解说。新提示词明确反对逐项读屏，允许有证据的潜台词、比喻和拟人，同时禁止凭空创造时间、天气、声音、气味、失败次数、身份、经历和画外结果。最终稿仍会校验词数、事件覆盖与故事阶段，失败时由编辑模型整篇重编。API 设置新增可选“最终故事编辑模型”，留空则复用故事生成模型；语音与画面模式保持原有科普压缩路线。

同日实测 60 秒目标时，GPT-5.4 虽生成了明显更完整的故事弧，但忽略 114-135 词要求，输出 337 词、约 149.8 秒。现已补齐硬性最大词数和精彩事件数量上限：60 秒默认规划约 12-20 个实际旁白事件，初稿超过 135 词或引用过多事件即自动整篇压缩重编；重编后仍超过约 151 词则拒绝结果。提示词同时要求只保留高价值转折，避免逐项枚举清理、检查和工具操作，并提醒英文逗号也是 GPT-SoVITS 分段点。

随后用户确认：60/90/120 秒不应成为破坏长片故事的硬上限，真正的产品限制是 Shorts 成片必须在三分钟内。现已将纯画面选项改为“期望时长”，允许故事为保持完整性而超过期望值，但生成结果的预计旁白与第四步实际导入配音均采用 179 秒安全上限；超限会重编或阻止生成预览。旁白时长估算曾短暂采用“约 2.1 词/秒且每单元至少 2 秒”，但这会把 `All right.` 等短语错误拉长；现已升级为单词数、英文音节数和标点停顿的混合估算，短语可自然低于 2 秒，完整长句随发音负担增长。打开旧项目时会在本地自动升级 `story.json` 的旧时间估算，不需要重新请求故事 API；参考 SRT 也会重新计算旧稿时长，导入真实同步 SRT 后仍以真实音频时间轴为准。337 词实测稿按最新算法约 149.5 秒，处于 Shorts 安全线内。

第四步新增“仅字幕测试预览”：无需导入 GPT-SoVITS 音频，程序会用最新估算生成参考 SRT、按该时间轴重新校准镜头；默认输出无声烧录字幕视频。真实 FFmpeg 回归结果为 12.03 秒、1922×1080、仅视频流、字幕已烧录；正式导入配音和同步 SRT 后仍走原有真实时间轴流程。

第四步另增项目级“保留原片声音”开关，默认关闭并随项目保存。开启后，程序会按每个粗剪镜头的源入点/出点同步裁剪原声音轨；仅字幕测试使用完整原声音量，已有英文配音时则以默认 0.22 音量将原声作为背景混入旁白。无音轨视频会拒绝开启。已用真实 12.03 秒视频分别验证 `original_clips` 和 `narration_with_original`，两种输出均包含 H.264 视频流与 AAC 音频流。

第四步按钮过多造成横向溢出后，操作区已拆成独立行并移除左侧占位空白。五分钟实测项目的原视频从桌面移动到 `D:\videoEdit` 后导致 FFmpeg `No such file or directory`；现于预览和抽帧前检查源文件，按同名与原始文件大小在项目/仓库附近自动恢复，否则弹出重新选择窗口，并用时长和分辨率校验是否为原视频。该实测项目已自动重新关联到现存文件，无需重新理解原片或生成故事。

字幕样式弹窗此前固定按 16:9 容器计算字幕、底板和拖拽坐标，竖屏原片使用 `PreserveAspectFit` 后会让这些元素跑进两侧黑边。现增加按源视频宽高比计算的实际视频视口，图片、字幕、底板、字号/边距缩放和拖拽均绑定该视口；导出逻辑未改。已用 544×960 实测项目离屏截图确认字幕完整位于竖屏画面内，QML 检查无错误。

两套应用的用户可见产物统一放到仓库根目录 `export/`。StoryCut V2 正式预览为 `<项目名>_storycut_final_preview.mp4`，仅字幕测试为 `<项目名>_storycut_subtitle_test.mp4`，GPT-SoVITS 参考字幕默认命名为 `<项目名>_gpt_sovits.srt`。SRT 保存对话框打开前会创建根 `export/` 并预填完整文件名；预览导出也会自动创建该目录。项目 JSON 保存从项目目录指向根 `export/` 的相对产物路径，加载时继续兼容旧的项目内 `export/` 和 `exports/rough_preview.mp4`。根 `.gitignore` 忽略整个 `export/`。StoryCut V1 现阶段仍使用自身 `output/`，后续可将其对外成品接入同一根 `export/`。

2026-08-17 仓库应用目录完成正式命名：旧一比一翻译工具由 `translator_studio/` 改为 `storycut_v1/`，长视频 AI 解说剪辑工具由 `commentary_studio/` 改为 `storycut_v2/`。启动器、更新器归档前缀、更新清单、依赖入口和文档已同步更名。本机数据仍保留在对应应用目录中，没有清理或覆盖用户项目。

StoryCut V2 新项目不再使用视频文件名，而是按创建日期自动命名为 `v2-MMDD`；当天已有同名项目时依次追加 `-1`、`-2`。首页顶部提供项目名编辑框，手动修改会同步重命名项目目录、项目 JSON 和最近项目显示；之后的根 `export/` 产物使用新项目名前缀。任务运行期间禁止改名，防止后台线程继续写入旧路径。

v0.1.7 / V1 v1.0.3 将两个应用的更新按钮升级为仓库级自动同步。`repository_updater.py` 直接下载 GitHub `main` 分支 ZIP，依据根 `update_manifest.json` 新增或替换两个应用、根启动器和文档，并删除旧清单或 `remove` 中已废弃的程序文件。严格保护 `.env`、`models/`、`venv/`、`projects/`、`output/`、`export/`、`cache/` 和 `config.user.yaml`，受影响程序文件先备份到 `.update_backups/`。旧 `commentary_studio/` 和 `translator_studio/` 保留最小化一次性更新桥接：旧版用户点击原更新按钮后，重启旧入口即会自动同步根仓库并打开新应用。

v0.1.8 / V1 v1.0.4 改为混合更新：先确认根 `.git`、Git 可执行文件、正确 `origin`、`main` 分支以及无受跟踪的本地修改，满足时在无 CMD 窗口的后台进程执行 `git pull --ff-only origin main`；任一检查不满足或 pull 失败时自动回退到 ZIP 清单更新。未使用 `git clean`、`reset --hard` 或强制删除。旧版首次桥接发布标签为 `storycut-v0.1.8` 和 `v1.0.4`；桥接会从当时的 `main` 归档读取真实目标版本，后续主分支继续升级时不会因桥接版本不同而失败。

2026-07-28 首页项目入口完成整理：

- 顶部“打开项目”改为“最近项目”，点击后弹出可滚动的最近项目列表。
- 每个项目可直接继续，也可经二次确认删除整个 StoryCut 项目目录及其缓存；原始视频不受影响。
- 删除接口严格限制在 `storycut_v2/projects/*/project.json`，任务运行期间拒绝删除。
- 原首页底部最近项目区已移除；手动选择 `project.json` 的入口和项目保存/删除说明移入弹窗。

2026-07-28 两个应用增加启动时静默更新检查：发现新版时只在原更新按钮显示可更新标识，不主动弹窗。根目录增加 `.vbs` 无 CMD 启动入口，原 `.bat` 保留用于排错。

本文后面的阶段记录保留作为历史背景；如与本节冲突，以本节和 `storycut_v2/README.md` 为准。

2026-07-27 仓库结构进一步整理为两个并列应用：

```text
storycut_v1/      旧的一比一翻译工具
storycut_v2/      StoryCut Studio（v0.1.0 封版）
models/                  共用模型
venv/                    共用虚拟环境
.env                     共用 API 配置
requirements.txt         共用基础依赖
```

旧工具的配置、输出、版本清单和更新器均属于 `storycut_v1/`；StoryCut 目录在此次整理中没有修改。

## 1. 项目背景

当前仓库原本是一个中文视频转英文二创工具：识别中文字幕、压缩翻译为英文、导入外部 TTS、调整音画节奏，并输出英文 Shorts。

旧工具采用一比一或近似逐句翻译。新需求是增加一个完全独立的 AI 解说剪辑工具，用于处理较长原视频：

1. 分析长视频的字幕、场景和画面。
2. 提取关键事件并生成 60～180 秒英文解说稿。
3. 为每句解说从原视频中匹配相关片段。
4. 生成可人工确认的粗剪时间轴。
5. 输出适合 YouTube Shorts 的竖屏视频。

新工具不与旧 Tkinter GUI 合并，产品名暂定 `StoryCut Studio`。

## 2. 已确认的架构方向

最终目标是单仓库、两个独立应用、共享大依赖：

```text
subdub-forge/
├── apps/
│   ├── translator/
│   └── storycut_v2/
├── shared/
├── models/
├── tools/
├── .env
└── README.md
```

为避免破坏旧程序，初版暂不移动旧工具，当前采用：

```text
video_translate_auto/
├── src/                         旧翻译工具，暂不移动
├── config.default.yaml
├── config.user.yaml
├── storycut_v2/           新解说工具
├── models/                      两个工具共用
├── venv/                        两个工具共用，不提交 Git
├── .env                         API 密钥共用，不提交 Git
├── .gitignore
└── README.md
```

待 StoryCut 稳定后，再迁移为最终的 `apps/` 结构。

## 3. GUI 技术选择

旧 GUI 使用 Python `Tkinter + ttk`，界面传统。

新 GUI 使用：

- Python 3.12
- PySide6 Essentials
- QML / Qt Quick
- Qt Quick Controls Basic 风格
- 深色现代界面

没有使用完整 `PySide6 Addons`，以控制依赖体积。目前新首页只需要 Essentials。

正式发布目标仍是 Windows 免安装便携目录，而不是要求用户自行安装 Python。开发阶段使用仓库根目录的 `venv`。

## 4. 当前新增文件

```text
storycut_v2/
├── main.py
├── config.default.yaml
├── requirements.txt
├── README.md
├── src/
│   ├── __init__.py
│   ├── app_controller.py
│   └── config_manager.py
└── ui/
    └── Main.qml

启动解说剪辑台.bat
CODEX_HANDOFF.md
```

此外修改了：

- `.gitignore`
- `启动图形界面.bat`

## 5. 当前已实现功能

### 5.1 StoryCut QML 首页

- 现代深色界面。
- 左侧流程导航。
- 创建项目与选择视频入口。
- 制作流程卡片。
- 最近项目列表。
- 分析、文案、镜头匹配、导出入口占位。

### 5.2 项目创建

导入视频后会在 `storycut_v2/projects/` 下创建：

```text
项目名称/
├── project.json
├── source/
├── analysis/
├── script/
├── timeline/
├── cache/
└── export/
```

`project.json` 当前保存：

- schema version
- 项目名称
- 原视频路径
- 创建时间
- 更新时间
- 当前阶段

### 5.3 项目恢复

- 启动时扫描最近项目。
- 首页显示最近项目。
- 可以选择已有 `project.json`。
- 恢复项目名称、视频路径和阶段状态。

### 5.4 独立配置

StoryCut 使用自己的：

- `storycut_v2/config.default.yaml`
- `storycut_v2/config.user.yaml`（本地文件，不提交）

共享：

- 根目录 `.env`
- 根目录 `models/`
- 系统或未来便携包中的 FFmpeg

## 6. Python 环境现状

之前旧 BAT 实际调用的是全局 Python，而不是仓库中的 `venv`。

现在已把以下依赖安装到根目录 `venv`：

- PyYAML
- faster-whisper
- openai
- charset-normalizer
- PySide6 Essentials
- 上述依赖的传递依赖

当前 `venv` 约 520 MB，已被 `.gitignore` 排除，不会上传 GitHub。

注意：`faster-whisper` Python 包不是模型权重：

```text
venv/Lib/site-packages/faster_whisper/   推理程序
models/faster-whisper/                   模型权重
```

两者都需要。模型权重仍由两个应用共用，没有重复下载。

全局 Python 中原有依赖尚未卸载，因为可能被其他项目使用。当前两个启动脚本已经不再调用全局依赖。

## 7. 启动脚本修复

以下脚本已改为强制使用 `venv/Scripts/python.exe`：

- `启动图形界面.bat`
- `启动解说剪辑台.bat`

曾发生 BAT 双击闪退，原因是 Windows `cmd.exe` 错误解析 UTF-8 中文提示及括号命令块。

现已改为：

- 纯 ASCII 错误提示。
- 无复杂括号命令块。
- 路径加引号。
- 异常时保留 CMD 窗口显示错误。

两个 BAT 都已实际启动验证，旧 GUI 和 StoryCut GUI 均能保持运行。

## 8. 已完成验证

- 旧工具依赖可从 `venv` 导入。
- PySide6 Essentials 可从 `venv` 导入。
- QML 页面可离屏加载并产生根窗口。
- 项目创建测试通过。
- 标准项目目录初始化测试通过。
- 最近项目扫描测试通过。
- 项目状态恢复测试通过。
- 两个 BAT 实际启动测试通过。

## 9. Git 状态

当前修改尚未提交。不要覆盖或清理这些修改。

预期状态包括：

```text
M  .gitignore
M  启动图形界面.bat
?? storycut_v2/
?? 启动解说剪辑台.bat
?? CODEX_HANDOFF.md
```

`venv/`、`models/`、`.env`、`storycut_v2/projects/` 均不应提交。

## 10. 开发进度与下一阶段计划

### 阶段 3：视频信息与预览

当前已完成：

- 自动发现配置路径、系统 PATH 和 `tools/ffmpeg/` 中的 FFmpeg/ffprobe。
- 使用 ffprobe 读取时长、分辨率、帧率、视频编码、音频编码和音轨数量。
- 使用 FFmpeg 抽帧生成 `cache/cover.jpg`。
- 将媒体信息持久化到 `project.json` 的 `media` 字段。
- 首页显示真实封面、时长、分辨率和编码信息。
- 媒体分析在后台线程执行，不阻塞 QML。
- FFmpeg 缺失时使用已有 PyAV 作为元数据和封面备用后端。
- 增加任务编号，防止快速切换项目时旧后台结果覆盖新项目。

本机已确认使用 WinGet 安装的 FFmpeg 8.1.1。真实视频验证结果：174.57 秒、1922×1080、30 fps、H.264/AAC，封面生成成功。

轻量时间定位已经完成：点击首页封面打开预览窗口，可拖动时间滑块或前后跳转 5 秒，FFmpeg 在后台抽取对应画面。真实视频的 0、30、90、170 秒定位均验证成功。该方案没有引入完整 PySide6 Addons。

仍未实现连续音视频播放。当前按时间抽帧已经能支撑场景检查和候选镜头定位；是否加入连续播放应等时间轴编辑需求明确后再评估。

原计划条目：

1. 使用 `ffprobe` 获取视频时长、分辨率、帧率、编码和音轨信息。
2. 将元数据写入 `project.json` 或独立分析文件。
3. 使用 FFmpeg 生成项目封面。
4. 首页显示真实时长、分辨率和封面。
5. 接入视频预览与时间定位。

建议优先确认本机 FFmpeg/ffprobe 路径，不要立即添加大型 Qt Multimedia Addons。可先复用 FFmpeg 生成代理视频、缩略图和帧预览，以控制依赖体积。

### 阶段 4：原片分析

当前已完成音频与语音转录链路：

- FFmpeg 提取 16 kHz、单声道 PCM WAV 到 `analysis/audio_16k_mono.wav`。
- 从共享 `models/faster-whisper/` 自动解析本地 large-v3 snapshot。
- 自动检测 CUDA，不可用时使用 CPU int8。
- 后台执行模型加载和全片转录，并向 QML 汇报进度。
- 输出 `analysis/transcript.json` 和 `analysis/transcript.srt`。
- 完成后将项目阶段更新为 `transcribed`，并在 `project.json` 记录产物路径。
- 使用真实视频 12 秒片段完成 large-v3 CPU int8 验证，模型加载、推理、JSON/SRT 输出全部成功。

下一项是场景切分和关键帧抽取。

场景与事件结构现也已完成：

- FFmpeg 使用场景变化阈值一次扫描视频并同时输出关键帧。
- `min_scene_sec` 过滤距离过近的切换，保留的时间戳与图片按原始帧序号严格绑定。
- 真实 174.57 秒视频检测到 36 个有效场景，输出 36 张关键帧，引用全部存在。
- 输出 `analysis/scenes.json`，包含场景起止时间、时长和关键帧路径。
- 将 Whisper 字幕按时间重叠分配到场景，输出 `analysis/events.json`。
- 事件数据预留 `visual_description`、`summary` 和 `importance` 字段，供后续视觉理解与故事组织填写。
- 完整分析结束后项目阶段更新为 `understood`。

关键帧视觉描述与事件检查界面也已实现：

- 使用 OpenAI 兼容视觉接口，默认 `gpt-4o-mini`，每批 4 张低细节关键帧。
- 只在用户主动点击“开始理解原片”后调用视觉 API；静态测试不会产生费用。
- 每批结果立即回写 `events.json`，保留已完成进度。
- API 未配置或不支持图片时只记录 warning，不破坏转录、场景和事件结果。
- 首页可展示前 12 个事件的关键帧、时间范围、原片字幕和视觉描述。
- 视觉 JSON 解析、事件数据模型和 QML 页面加载均已验证。

第 1 步的工程链路已经完整，下一步进入第 2 步“组织故事”：根据 `events.json` 生成可选择的故事结构和英文解说稿。

## 第 2 步当前进度

“组织故事”初版已实现：

- `story_service.py` 将 `events.json` 压缩为模型输入。
- 支持 60/90/120/180 秒目标上限，按约 2.25 英文词/秒控制词数。
- 明确要求信息不足时缩短成片，不重复、不虚构。
- 输出 `script/story.json`，包含标题、切入角度、开场钩子、保留/舍弃事件、故事大纲和逐句英文解说。
- 每句旁白绑定 `event_ids` 和中文 `visual_query`，为第 3 步镜头匹配准备。
- 对 AI JSON 进行事件 ID、词数和预计时长规范化。
- GUI 提供目标时长选择、生成进度、大纲和逐句英文解说编辑。
- 编辑英文解说后失去焦点会自动写回 `story.json`。
- 修复同名视频重新导入时重置已有项目阶段和产物的问题。
- 模拟故事数据与 QML 界面验证通过，尚未在自动测试中调用用户 API。

## 第 3 步当前进度

“匹配镜头”初版已实现：

- `matching_service.py` 使用故事稿中的 `event_ids` 作为首要依据，并结合已有视觉描述和字幕做本地相似度排序。
- 该阶段不额外调用 API，每句解说最多给出 5 个原片候选镜头。
- 匹配结果保存到 `timeline/matches.json`，包含候选场景、起止时间、分数、原因和当前选择。
- GUI 展示每句解说对应的候选关键帧；单击替换当前镜头，双击打开对应时间点的原片预览。
- 项目重新打开时自动恢复镜头选择，生成后项目阶段更新为 `matched`。
- 使用真实 `2.mp4` 项目验证：3 个场景、2 句解说分别首选场景 1 和场景 2，QML 成功加载 2 条匹配结果。
- 单个场景短于解说时会按相邻优先原则自动补入其他候选场景，支持一条解说组合多个镜头。
- GUI 提供当前主镜头入点/出点 ±0.5 秒微调，并实时显示镜头覆盖时长是否足够。
- 每次生成、候选替换和边界调整都会同步输出 `timeline/rough_cut.json`，其中同时记录源视频时间和最终成片时间。
- 真实 `2.mp4` 粗剪验证为 2 句解说、2 个镜头、总时长 8.5 秒、覆盖率 2/2；另以短场景数据验证了自动组合双镜头。

第 3 步的可用初版已经完成。下一步进入第 4 步：先实现无配音的粗剪预览视频，再接英文配音、字幕和 1080×1920 导出。

## 第 4 步当前进度

已接入与旧项目一致的外部 GPT-SoVITS 流程：

- `voice_service.py` 从 `story.json` 生成 `script/tts/gpt_sovits_reference.srt`，不再生成项目和 GPT-SoVITS 都不使用的编号/纯文本 TXT。
- GUI 第 4 步提供“导出 SRT 到 GPT-SoVITS → 导入英文配音 → 导入同步 SRT”三个入口。
- 导出按钮使用系统另存为对话框将参考 SRT 复制到用户指定位置。
- 导入音频时用 FFmpeg 统一转换为项目内 `audio/narration.wav`（48 kHz、单声道 PCM），并通过 ffprobe 读取真实时长。
- 同步 SRT 保存为 `audio/narration.srt`；存在同步 SRT 时逐句校准，不存在时按句子预计时长比例分配整段音频。
- `apply_voice_timing()` 会按真实配音时长重新选择/补齐镜头，并重建 `timeline/rough_cut.json`。
- 粗剪预览合成已改为使用英文配音音轨，而不是保留原片声音。
- 使用项目 2 生成了 2 句 GPT-SoVITS 文案包；以 10 秒模拟真实配音验证时间线变为 4.8 + 5.2 秒、总计 10 秒且覆盖完整，随后已恢复 8.5 秒未导入状态。
- QML 无窗口加载通过，能识别文案包已存在、英文音频尚未导入。

下一步需要用户用 GPT-SoVITS 生成一份实际英文音频（推荐同时导出同步 SRT），在 GUI 导入后完成真实音频合成验证。之后继续字幕烧录、原声背景音量选项和 1080×1920 导出。

用户已为项目 2 导入实际 GPT-SoVITS 音频和 SRT，后续要求确定为：

- TTS SRT 按句号、问号、感叹号拆分；当前 2 条故事解说对应 4 条实际字幕。
- 不打开旧 GUI，而是把旧项目成熟的字幕/编码逻辑接入 StoryCut。
- 完全去除原声，只使用英文配音。
- 默认面向手机 YouTube Shorts，输出 1080×1920 竖屏。

已完成并实际验证：

- 导出的 GPT-SoVITS 参考 SRT 现在直接生成 4 条标点断句。
- 第二步的创作提示已改为“高信息密度的母语科普 YouTube 作者”：模型先静默理解事实、角度和情绪，写成自然连贯的完整旁白，再执行反直译、反营销、反作文、反纪录片包装腔和反 AI 套路自检。
- 技术性 GPT-SoVITS 拆分由程序在生成后完成，不再迫使模型为了字幕写成连续机械短句；逗号、句号、问号、感叹号、分号和冒号后的内容会成为独立解说/镜头匹配单元。
- 默认故事风格为 `natural_science_youtube`，生成温度为 `0.55`；时长和词数只作为上限，每句话必须增加信息或推进解释，不再强制戏剧化问题、冲突或结果开头。
- GPT-SoVITS 断句兼容 `today?Let's`、`enthusiasts,but` 等标点后无空格文本，同时避免拆开 `3.5`、`1,000`、`12:30` 等数字。
- 实际 4 条 SRT 通过文本相似度正确归并回 2 条故事解说，真实分段为 5.066 秒和 4.094 秒。
- 最终时间线 9.16 秒、3 个镜头、覆盖完整。
- FFmpeg 输出 H.264 1080×1920 视频 + 48 kHz 单声道 AAC 英文配音，完全不映射原片音轨。
- 默认在竖屏裁切前裁掉横屏源视频底部 18% 的原字幕带。
- 复用旧项目 ASS 中间字幕思路，显式写入 1080×1920 PlayRes；英文字幕字号、换行、描边和手机安全边距已通过抽帧检查。
- 实际输出 `exports/rough_preview.mp4` 为 9.166667 秒、约 4.86 MB；视觉检查确认原中文字幕已移除、英文字幕大小合理。

随后用户修正默认画面策略：Shorts 仍采用 1080×1920 画布，但横版原视频必须保持完整宽高比并自动居中，类似抖音发布横版视频的视窗效果；当前不要自动裁切，裁切以后作为可选功能。

已据此调整并重新验证：

- `export.fit_mode` 默认改为 `contain`。
- 原片使用 `force_original_aspect_ratio=decrease + pad` 完整居中，不放大裁边。
- `cleanup_original_subtitles` 默认关闭，不再以裁掉底部 18% 的方式清理原字幕。
- 英文 ASS 字幕放在横版画面下方的黑色安全区域，避免与原片字幕重叠。
- 项目 2 重新输出为 1080×1920、9.16 秒、约 3.09 MB；抽帧确认横版画面完整、居中、无裁切，英文字幕位于下方安全区域。

用户再次澄清：“不裁剪”也不代表创建 1080×1920 黑底画布。默认应完全保持原视频自身宽高和画面，发布平台会在 Shorts/抖音视窗内自行居中；当前不要进行任何缩放、补边或裁切。

最终默认行为已纠正为：

- `export.fit_mode: original`。
- FFmpeg 视频链路不包含 `scale`、`pad` 或 `crop`。
- ASS `PlayResX/PlayResY` 使用源视频真实分辨率。
- 项目 2 已重新输出并由 ffprobe 确认为 1922×1080，9.16 秒，约 6.05 MB。
- 抽帧确认没有黑色竖屏画布、没有画面裁切；英文字幕位于原中文字幕上方。
- 1080×1920 画布、铺满裁切和字幕遮罩以后只能作为明确的用户可选项，不能作为默认。

字幕样式与预览面板现已完成：

- 第 4 步新增独立 QML 弹窗，左侧用项目封面即时预览，右侧为设置；没有重新打开旧 GUI。
- 提供半透明底、清爽描边、Shorts 大字三个预设。
- 支持字体、字号、底部距离、左右边距、粗体、半透明背景、背景浓度、背景留白和描边粗细。
- 支持原字幕不处理、黑色遮罩、区域模糊、Delogo；位置、高度和遮罩浓度可调，默认仍为不处理。
- 设置保存在当前项目 `project.json -> settings.subtitle`，重新打开项目后恢复。
- FFmpeg 生成 ASS 时使用同一份项目设置，支持 BorderStyle 1 描边与 BorderStyle 3 半透明背景。
- 项目 2 应用默认“半透明底”预设：48 px、底部 150 px、62% 黑底、12 px 留白；实际抽帧确认英文字幕处于下方且与原字幕上下错开。
- 黑色遮罩模式已用实际 9.16 秒项目完成 FFmpeg 合成验证。

字幕样式弹窗曾出现布局问题：左侧预览区的隐式宽度撑满整行，把右侧设置面板挤到弹窗外。现已给左右区域设置明确的 minimum/preferred/maximum width，并将 ScrollView 固定为 `contentWidth: availableWidth`。1280×800 实际弹窗截图确认左右两栏与滚动设置均正常显示。

1. 提取音频。
2. 使用 Faster-Whisper 生成带时间码字幕。
3. 使用 FFmpeg 或轻量算法进行场景切分。
4. 每隔一定时间或按场景抽取关键帧。
5. 生成 `analysis/transcript.json`、`analysis/scenes.json`。
6. 后续再接视觉模型生成画面描述。

### 阶段 5：事件与解说文案

1. 将字幕、场景和视觉描述整理为事件列表。
2. AI 选择值得保留的故事线。
3. 根据 60/90/120/180 秒目标生成英文解说。
4. 解说必须拆成逐句结构，每句包含视觉需求和候选原片时间。
5. 保存为 `script/commentary.json`。

### 阶段 6：镜头匹配和粗剪

1. 字幕语义匹配。
2. 视觉描述匹配。
3. 保持合理的原片时间顺序。
4. 每句解说提供多个候选镜头。
5. 人工确认、替换和调整起止点。
6. 保存为 `timeline/rough_cut.json`。

### 阶段 7：导出

1. 生成英文旁白。
2. 处理字幕。
3. 自动竖屏裁切与主体跟踪。
4. FFmpeg 合成 1080×1920 Shorts。
5. 输出到项目 `export/`。

## 11. 开发原则

- 不修改或破坏旧翻译流程。
- StoryCut 保持独立配置、独立输出和独立启动入口。
- 大模型和 FFmpeg 尽量共享。
- GUI 与耗时处理逻辑分离。
- 所有耗时任务必须在后台执行，不能阻塞 QML 主线程。
- 每个阶段产生持久化文件，支持关闭后继续。
- 第一版目标是 AI 自动粗剪 + 人工确认，不追求完全无人值守。
- 不要为了界面功能轻易加入 Qt WebEngine、Qt Quick 3D、Charts 等大型模块。
- 新增依赖前评估安装体积和便携打包影响。

## 12. 给后续 Codex 的建议起点

开始前先执行：

```powershell
git status --short
Get-Content CODEX_HANDOFF.md
```

然后检查：

```powershell
venv\Scripts\python.exe -c "import yaml, faster_whisper, openai; from PySide6.QtQuickControls2 import QQuickStyle; print('OK')"
```

当前不要从早期阶段重新开发。先阅读本文第 0 节和 `storycut_v2/README.md`，运行回归检查后，再从 v0.1.0 后续增强开始；也不要移动旧项目目录。
