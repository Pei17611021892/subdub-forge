# StoryCut Studio 当前开发交接

更新时间：2026-09-09

## 当前状态

- 正式版本：`2.1.4`
- 稳定主流程：从视频开始的“理解原片 → 组织故事 → 匹配镜头 → 预览导出”初版已经通过，后续仍持续修复 Bug 和增加合理功能。
- 开发中流程：从纯文稿开始，目前属于开发预览，尚未形成完整可交付闭环。
- 2.x 视频流程已修复派生产物失效链路：项目切换会完整重置内存状态；重新理解、生成故事、匹配镜头、导入配音/SRT、生成去字幕视频以及修改画布或字幕样式后，旧预览和旧质检会解除绑定，项目重开也不会恢复失效文件。旧导出文件保留，不自动删除。
- 视频镜头匹配已改为全局非重复分配：导入配音或同步 SRT 后按最终时长整体重排，同一事件跨句使用时连续顺播，素材不足时切换候选，不再逐句从事件起点重放。手动换镜头也会重绑后续时间线。
- 故事生成会检查连续三句以上复制同一事件列表与宽泛 `visual_query` 的情况，并要求 AI 重新细化到逐句可见主体、物体和动作；关键帧视觉描述缺失时会明确提示语义匹配精度受限。
- 成片检查现会阻止任何原片区间重叠回放，并提醒故事镜头绑定过宽。跨电脑项目若原片绝对路径失效，但项目内去字幕完整视频仍在，可继续质检、预览和导出；重新理解或重做去字幕仍要求关联原片。
- 原片理解不再把视觉接口失败伪装成完成：普通接口故障仍持久化失败状态并允许保留本地结果重试；批量请求遇到 `content_policy_violation` 时自动拆批定位并跳过单张受限画面，其余关键帧继续完成。完成后集中显示失败缩略图、场景编号和时间，单击可用系统看图程序打开，也可仅重试尚无描述的失败画面；重试仍失败不会清空现有故事，确实补回新描述后才使旧故事和镜头失效。状态文字已限制在卡片宽度内。
- 纯文稿素材匹配纵向切片已进入工作区：本地与 Pexels/Pixabay 下载素材统一入库，缺失文件可恢复显示；后台增量生成场景、缩略图和保守基础标签，再按全部分镜全局分配候选。界面显示高/中/低置信度、原因、替代候选和补素材提示，手动选择会锁定且重新匹配不覆盖。
- 纯文稿多素材导出已接通：`rough_cut.json` 可混合多个视频文件和静态图片，视频按入出点裁剪、图片按镜头时长循环，统一复用配音、同步 SRT、字幕样式、竖屏画布、成片检查和根 `export/` 导出。多素材默认不混合来源原声，也不显示单原片的去字幕步骤。
- 纯文稿候选支持双击调用系统默认播放器或看图程序打开原素材，不引入额外 Qt Multimedia 打包依赖；候选卡片使用可收缩等宽布局，不会被素材尺寸撑宽主页面。控制器仍保留场景范围抽帧预览能力。
- 最近项目用颜色、图标和文字徽标醒目区分视频与纯文稿；视频项目首页封面改为调用系统默认播放器打开当前可用视频。每次恢复已有故事的项目时“组织故事”默认收起；文案审查可单独折叠，应用过任意建议时默认收起。
- 文案审查折叠使用统一详情容器，委托项不会在收起后继续占位或覆盖故事内容；应用单条建议会保留其余未应用项，过期状态摘要也会明确显示仍保留的数量。只有主动重新运行文案审查才会用新一轮报告替换旧报告。
- 当前阶段文档：`docs/phases/manuscript-workflow.md`
- 上一正式发布提交：`b8d3fe7 fix: release StoryCut v2.1.3 updater compatibility`

## 版本策略

不再规划 `3.0.0` 版本。StoryCut 继续沿用当前版本序列小步发布：`2.1.1` 之后依次升级为 `2.1.2`、`2.1.3` 等。纯文稿流程完成闭环后只移除开发预览标记，不跳升大版本；视频流程与纯文稿流程都在同一版本序列持续维护。

## 2.1.4 已完成

- 关键画面视觉请求遇到内容安全限制时自动把失败批次拆到单场景，只跳过受限画面，不再让一张图终止整批或反复卡住重试。
- 成功描述按批持久化；重试只请求尚无描述的失败画面，不重复消耗已成功画面。
- 理解原片区域集中展示失败画面缩略图、场景编号和时间，可打开原图并再次重试；长错误文本不会再撑出页面。
- 失败画面再次被限制时保留现有故事，只有重试实际补回新描述才使下游故事和镜头失效。
- 根更新清单开始纳入 `AGENTS.md`；需要 2.1.3 或更新版 ZIP 更新器，Git 克隆用户不受该版本门槛影响。

## 2.1.3 已完成

- 修复旧版 ZIP 更新器拒绝根清单中 `AGENTS.md` 和 `docs/` 开发文档、导致 `2.1.2` 安装中止的问题。
- `2.1.3` 根清单只包含 `2.1.1` 安全白名单已允许的根文件及 `storycut_v2/` 程序文件；新版更新器同时授权受管开发文档，供后续版本使用。
- 新增发布清单向后兼容测试，避免新增根文件再次阻断旧客户端更新。运行程序功能与 `2.1.2` 相同。

## 2.1.2 已完成

- 纯文稿项目统一管理本地与 Pexels/Pixabay 下载素材，后台增量分析视频场景和图片，持久化缩略图、基础标签、来源、许可及缺失状态。
- 分镜按全部素材全局自动匹配，展示绿/黄/红置信状态、匹配原因、替代候选与补素材提示；用户选择可锁定并在重新匹配时保留。
- `rough_cut.json` 支持混合多个视频和静态图片，复用现有配音、同步 SRT、字幕样式、画布、成片检查和最终导出；FFmpeg 多来源实测通过。
- 候选素材和视频项目首页封面可调用系统默认播放器或看图程序；最近项目增加醒目的视频/纯文稿类型区分。
- 已有故事项目恢复后默认折叠“组织故事”；文案审查可独立折叠，修复折叠内容重叠，并确认应用建议不会删除其他未应用项。

## 2.1.1 已完成

- 建立统一派生产物失效记录；重新理解、重新生成故事、重新匹配、导入配音/SRT、重做去字幕、修改画布或字幕样式后，旧文件保留但解除项目绑定，旧成片不会再参与新预览的前置检查。
- 镜头分配从逐句独立截取改为全局非重复分配；同一事件跨句时连续顺播，素材不足时切换候选，重新匹配会自动重新套用已有真实配音和同步 SRT。
- 故事生成会识别连续多句复制同一宽泛镜头要求，成片检查会阻止原片区间重叠回放，并在视觉描述不足时提示语义匹配风险。
- 视觉理解失败保存为部分失败状态，重开项目后仍明确显示；可只重试关键帧视觉和分层理解，成功后使旧故事及后续产物失效。
- 跨电脑项目在原始绝对路径失效时，可使用项目内已有去字幕完整视频继续质检、预览和导出。

## 2.1.0 已完成

- 第四步按真实操作顺序重构为配音文稿、配音/SRT、清除原字幕、成片画布、字幕样式、合成与测试。
- 新增项目内去字幕中间视频，支持黑色遮罩、区域模糊和 Delogo；后续预览与合成优先使用中间视频。
- 是否烧录字幕会同步显示或隐藏字幕样式步骤，并控制最终 FFmpeg 字幕链路。
- 字幕可独立开启背景并选择底板效果。
- 成片画布支持裁剪拉伸、竖屏完整和原始比例；裁剪拉伸支持多种宽高比、固定比例/自由缩放、四角拖动、复位、边缘吸附和最高 1000% 缩放。
- 仅字幕测试与最终成片预览状态分离；只有最终成片预览可以导出。
- 根、子版本号和更新清单统一为 2.1.0，新增服务已进入更新清单。

## 当前下一目标

继续纯文稿流程的真实项目收口，优先完成：

1. 选择一个用户真实纯文稿项目，完成分镜、素材准备、分析匹配、配音/SRT、检查、预览和最终导出全过程。
2. 用真实素材继续校准 FFmpeg 场景阈值、长素材耗时、损坏文件继续处理和缩略图质量。
3. 在真实项目中继续打磨缺失或低置信镜头的替换效率和预览体验。
4. 根据真实项目结果校准绿/黄/红阈值、补素材提示和素材集中度惩罚。
5. 完成真实素材闭环后，再评估低置信候选的批量视觉理解。

具体范围与验收标准只读 `docs/phases/manuscript-workflow.md`，不要从历史交接重新推断需求。

## 重要兼容约束

- 当前应用目录仍叫 `storycut_v2/`，这是内部兼容路径，不代表产品只能停留在 2.x。
- 不恢复 V1。用户数据、项目、模型、密钥、个人配置和根 `export/` 必须受保护。
- 应用更新优先 Git 快进；不可用时使用 ZIP 清单同步，不清理未知文件。
- GPT-SoVITS 暂不内嵌，继续使用导出文稿/SRT、外部生成、导入真实配音和同步 SRT 的流程。
- 成片面向 YouTube Shorts，硬上限低于 180 秒。

## 验证基线

当前回归结果：133 项自动化测试通过，QML 离屏加载成功。新增覆盖视觉批次遇到内容安全限制后自动拆分、保留安全画面并只跳过单张受限帧、只重试尚无描述的失败画面，以及项目重开后恢复失败缩略图列表；发布清单测试按 2.1.3 起支持的受管根文件白名单校验，并确认包含 `AGENTS.md`。`v2-0909` 实际失败记录为 `gpt-4o-mini` 返回 `content_policy_violation`，接口与 FFmpeg 正常，失败前已完成 4/75 张关键帧；2.1.4 重试会从剩余 71 张继续并隔离受限帧。FFmpeg/ffprobe 8.1.1 的场景切分和多素材竖屏合成实测继续有效；用户真实项目的完整闭环仍待验证。

常规回归：

```powershell
cd storycut_v2
python -m unittest discover -s tests -p "test_*.py"
```

如果本机项目虚拟环境解释器路径失效，可使用 Codex 工作区 Python，并把根 `venv\Lib\site-packages` 加入 `PYTHONPATH`。不要因此把依赖安装到系统全局。

## 历史资料

旧的完整交接已归档到 `docs/archive/DEVELOPMENT_HISTORY.md`。该文件包含早期方案、废弃决策和大量验证过程，只在定位历史原因时按关键词读取局部，不要在新任务开始时全文加载。
