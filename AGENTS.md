# StoryCut 开发规则

## 开始任务

1. 先执行 `git status --short`，保留并理解现有未提交修改，不覆盖、不清理用户工作。
2. 完整阅读根目录 `CODEX_HANDOFF.md`。
3. 只读取交接文档中“当前阶段文档”指向的文件；按需搜索代码。除非追查历史决策，否则不要读取 `docs/archive/`。

## 产品边界

- StoryCut 的稳定视频流程持续维护：理解原片、组织故事、匹配镜头、配音/字幕与成片导出。
- 成片主要面向 YouTube Shorts，最终时长不得超过 3 分钟；单集模式必须优先交付可编辑结果，不能因原片信息过密直接失败。
- 纯文稿流程当前是开发预览；完成文稿、分镜、素材分析与自动匹配、配音导出的可用闭环后，再转为正式功能。
- 不再以 `3.0.0` 表示纯文稿完成。版本继续沿用当前序列小步递增，例如 `2.1.1`、`2.1.2`、`2.1.3`；视频流程的 Bug、新需求和纯文稿能力可在同一版本序列持续发布。
- 不恢复已删除的 V1，不随意移动内部兼容目录 `storycut_v2/`。

## 实施与验证

- 保护 `.env`、`config.user.yaml`、`projects/`、`models/`、`export/`、缓存和用户素材。
- 耗时工作放到后台，不能阻塞 QML 主线程；项目阶段产物应持久化并支持重新打开。
- 优先复用 FFmpeg 和现有依赖，新增大型依赖前先评估体积与便携性。
- 修改后执行相关测试、QML 加载检查和 `git diff --check`；未经用户要求不要提交或推送。

## 文档职责

- `README.md`：用户说明、版本记录、整体路线图。
- `CODEX_HANDOFF.md`：短期当前状态、下一目标和验证信息，保持简短并在实质开发后覆盖更新。
- `docs/phases/`：当前或未来大阶段的详细需求与验收标准。
- `docs/archive/`：历史记录，默认不读、不继续追加。
- 上级目录的 `todo.md` 和 `codex交接方式.md` 不属于仓库，也不是项目事实来源。
