# StoryCut

StoryCut 是一款 Windows AI 解说剪辑工具，用于把较长的视频整理为 3 分钟以内、适合 YouTube Shorts 发布的英文解说成片。

仓库只维护 StoryCut 一个应用。当前已完成“理解原片 → 组织故事 → 匹配镜头 → 预览导出”的完整流程，支持语音视频、纯画面视频、长片分层理解、故事保真与自动拆分、文案审查、GPT-SoVITS 外部配音、字幕样式、成片质量检查和安全更新。

```text
storycut_v2/    StoryCut 应用代码（内部目录名为兼容旧项目路径而保留）
models/         共用 Faster-Whisper 模型，不提交
venv/           Python 虚拟环境，不提交
.env            API 地址与密钥，不提交
export/         用户可见的统一导出目录，不提交
requirements.txt
```

## 安装

在仓库根目录创建虚拟环境并一次安装全部依赖：

```powershell
python -m venv venv
venv\Scripts\python.exe -m pip install -r requirements.txt
```

根目录 `requirements.txt` 已引用 StoryCut 的依赖清单，不需要重复安装子目录清单。

还需要安装 FFmpeg 和 ffprobe，可加入系统 PATH，也可在应用配置中指定绝对路径。Faster-Whisper 模型位置和自动下载方式参见 [MODEL_DOWNLOAD.md](MODEL_DOWNLOAD.md)。首次理解原片时如缺少配置模型，程序默认显示下载进度，完成后自动继续；也可在 `storycut_v2/config.user.yaml` 中关闭自动下载：

```yaml
analysis:
  auto_download_model: false
```

## 启动

双击根目录中的：

```text
点我启动StoryCut（AI解说剪辑）.vbs
```

启动器使用 `pythonw.exe`，正常运行时不会显示 CMD 窗口。也可在根目录运行：

```powershell
venv\Scripts\python.exe storycut_v2/main.py
```

程序启动后会在后台静默检查更新；发现新版时，首页更新按钮会显示绿色可更新标识，不会自动弹窗打断操作。

## 功能说明

- “最近项目”用不同颜色、图标和“视频 / 纯文稿”徽标区分两种项目；视频项目首页封面可直接调用系统默认播放器打开当前可用视频。

### 1. 理解原片

- 使用 FFmpeg/ffprobe 读取视频信息、生成封面、检测场景并提取关键帧。
- “语音与画面”使用本地 Faster-Whisper 转写中文语音，再用关键画面补充上下文。
- “纯画面叙事”跳过语音转写，分析人物动作、姿态、工具交互、环境、障碍、变化和场景衔接。
- 默认自动执行分层理解：短视频跳过冗余综合调用，长视频分片分析后建立全片章节、关键事实、转折和跨场景联系；失败时自动回退基础理解。
- 视觉请求同时识别清晰可见的文字、表格、图表趋势、图示关系和公式；看不清的内容不会猜测。
- 重要但不确定的技术画面可抽取高清原帧复查，默认最多 6 个，没有需要复查的内容时不增加请求。
- 处理过程默认显示动态状态；只有取得持续且稳定的实测速度后才显示预计剩余时间。

### 2. 组织故事

- “语音与画面”把原片内容压缩为自然的英文科普解说，保留关键机制、因果、证据、转折和结论。
- “纯画面叙事”先通读全片事件生成故事规划，再由最终故事编辑模型写成具有环境、动作、动机和情绪线索的英文解说。
- 支持自动叙事策略，也可手动指定；“不设置”保持基础故事逻辑。
- 支持 60、90、120、180 秒目标，单集硬上限为 179 秒，为 Shorts 三分钟限制留出余量。
- 未开启分集时，即使模型重编仍然超时或留下零碎语音单元，也会继续执行本地重要度裁剪和断句合并，优先交付一条三分钟内、可继续编辑的完整草稿，而不是让整个故事生成失败。
- 英文解说可直接编辑并自动保存；参考 SRT 按 GPT-SoVITS 标点规则拆句。
- 旁白时长综合英文单词、音节、标点停顿和历史真实配音语速估算；第四步导入配音后会把当前故事时长自动改为真实值，有同步 SRT 时再精确到每句，并使用原速配音样本校准后续项目。
- 可选“文案审查”一次检查事实、原片证据、因果、数字单位和术语一致性；应用建议后重建 SRT，并在已有时间线时自动重匹配镜头。
- 重新打开已有故事的项目时，“组织故事”默认收起；文案审查可单独展开或收起，应用任意建议后自动收起。
- 长片故事保真与拆分建议使用多信号判断，包括时长、事件数、中译英容量、持续语音密度、有效信息点、技术证据、关键事实/转折和单集时长压力。默认不拆分并优先提炼为一条三分钟内视频；用户在第一步主动允许后，才会在单集确实讲不完整时拆成最少数量的独立分集。

### 3. 匹配镜头

- 根据事件绑定、字幕、视觉描述、时间顺序和重复使用情况，从全部场景中自动匹配镜头。
- 每句解说保留多个候选；正常流程无需逐句手选。
- 候选替换和 0.5 秒入点/出点调整位于“高级调整”。
- 镜头不足时可组合多个场景，时间线会随项目保存。
- 自动拆分后的每个分集拥有独立故事和镜头时间线，仍沿用同一套匹配流程。

### 4. 配音、字幕与预览导出

- 第四步按实际操作顺序拆为六个小步骤：导出配音文稿、导入配音与同步 SRT、清除原字幕、设置成片画布、设置字幕样式、合成与测试。
- 可按需导出 GPT-SoVITS 使用的一句一行 TXT 或带预估时间轴的参考 SRT，再导入生成的英文音频和同步 SRT。
- 缺少同步 SRT 时，可按需用共享 Faster-Whisper 模型重新识别英文配音。
- 真实配音超出 179 秒时，先在最高 1.25x 范围内安全加速；仍超限时按本次真实语速生成精简方案。
- 应用精简前自动归档旧故事、配音和同步字幕，恢复时先并排对比，再由用户确认。
- 没有 TTS 环境时，可生成无声音或保留原声的“仅字幕测试预览”。
- 默认移除原片声音；“保留原片声音”是可选项。
- 成片画布提供“裁剪拉伸、竖屏完整、原始比例”：裁剪拉伸可选择适应、16:9、4:3、1:1、3:4、9:16 盒子比例，并通过四角拖动、固定比例或自由拉伸调整居中画面。
- 裁剪拉伸支持一键复位；固定比例时宽高同步变化，自由拉伸时宽度和高度可分别调整。视频宽度或高度接近画布边缘时会精确吸附；白色选框和四角控制点可显示在画布盒子外，缩放范围最高 1000%，最终 FFmpeg 合成使用与弹窗一致的参数。
- “是否烧录字幕”同时控制字幕样式步骤：开启时显示第 5 步、合成为第 6 步并应用全部字幕样式；关闭时隐藏字幕样式、合成为第 5 步且不写入字幕。
- 内置标准字幕、清爽描边、Shorts 大字、纪录片暖白、科普黄字、极简白字预设。
- “清除原视频字幕”独立生成项目内的去字幕中间视频，支持黑色遮罩、局部模糊和 Delogo；后续画布、英文字幕与合成自动使用该视频。
- 去字幕中间视频可直接用系统播放器检查，也可在确认后删除；删除后后续步骤自动恢复使用原视频。
- 英文字幕样式与原字幕清除互相独立，不会在设置原字幕遮罩时显示或生成英文字幕。
- 字幕预览按最终成片画布显示，支持方向锁定拖动、中心/边缘吸附、滑块和精确数值输入，也可生成包含竖屏背景的 FFmpeg 真实效果预览。
- 成片检查会检查项目完整性、时长、镜头覆盖、空隙、重叠、越界、闪切、重复使用、字幕时间线、黑画面、静音、异常音量、编码和音轨。
- FFmpeg 完成后再使用 ffprobe 验证真实输出，诊断命令和滤镜保存到根目录 `export/_internal/`。
- 成片预览生成后可点击“导出成片”选择保存位置，无需手动进入导出目录复制文件。

## API 与模型配置

首页“API 设置”可管理共享 `.env` 中的 `OPENAI_API_KEY` 和 `OPENAI_BASE_URL`，无需手动寻找配置文件。接口地址应填写 OpenAI 兼容 API 根地址，通常以 `/v1` 结尾，不要填写服务商网页或完整的 `/chat/completions` 地址。

界面可分别选择故事生成、最终故事编辑和视觉理解模型。“选择模型”会异步读取兼容接口的 `/models` 列表；接口不支持列表时仍可手动输入。故事生成与最终编辑还可分别设置模型思考强度，不支持所选档位的接口会自动逐级降档。纯画面模式可让经济型视觉模型承担批量理解，让更强的文本模型负责全片故事编辑。

## 本地数据与项目结构

以下内容受 Git 忽略和内置更新器保护，不会提交或被版本更新覆盖：

- `.env`
- `models/`
- `venv/`
- `storycut_v2/config.user.yaml`
- `storycut_v2/projects/`
- `storycut_v2/cache/`
- `export/`
- `.update_backups/`

每个 StoryCut 项目主要包含：

```text
analysis/     转写、场景、关键帧、事件和分层结构
script/       故事规划、英文解说、审查结果和参考 SRT
timeline/     镜头匹配和粗剪时间线
audio/        英文配音、同步字幕及原始导入备份
archive/      改稿、精简或恢复前的可恢复版本
cache/        封面与项目预览缓存
project.json  项目状态、项目名和字幕设置
```

用户需要查找的成品统一位于根目录 `export/`，并使用项目名作为文件名前缀。

## 更新方式

StoryCut 启动后会静默检查 GitHub 版本。有新版时，用户确认后：

1. 正常 Git 克隆、位于 `main` 分支且受跟踪程序文件没有本地修改时，后台执行快进更新。
2. 没有 Git、不是克隆目录或 Git 更新不可用时，自动使用 ZIP 清单同步。

两种方式都不会执行 `git clean`、`reset --hard`、强制重置或删除未知文件。ZIP 更新会先把将要变更的程序文件备份到 `.update_backups/`，并保护本地项目、配置、模型和导出文件。

## 当前边界

- GPT-SoVITS 暂不内嵌，继续使用“导出参考 SRT → 外部生成 → 导入配音和同步 SRT”的流程。
- Delogo 使用传统 FFmpeg 周边像素插值，复杂动态背景可能出现拉伸纹理。
- 当前以单项目人工确认流程为主，尚未支持批量任务。
- 自动竖屏裁切和主体跟踪不属于默认行为。

## 版本记录

- `v2.1.2`：纯文稿开发预览完成本地与下载素材的增量分析、全局自动匹配、置信提示与锁定，并接通多来源视频/图片、配音、字幕、画布、成片检查和导出；候选素材和视频项目封面可调用系统播放器。最近项目增加类型标识，已有故事和文案审查支持可靠折叠，未应用审查建议继续保留。
- `v2.1.1`：修复项目重新打开及上游修改后的派生产物清理与重绑，旧预览不再阻止生成新成片；导入配音/SRT 或重新匹配后按真实时长全局分配镜头，同一原片区间不会跳回重播。视觉理解失败会持久显示、阻止错误故事继续生成，并可保留转写和场景结果单独重试；跨电脑项目可使用项目内去字幕视频继续预览和导出。
- `v2.1.0`：重构预览导出流程，增加独立原字幕清除中间视频、字幕烧录联动和字幕背景效果；成片画布支持多种比例、固定或自由裁剪拉伸、边缘吸附及最高 1000% 缩放。纯文稿功能继续作为开发预览，不计入本次正式功能范围。
- `v2.0.9`：统一仓库与应用版本号；修复嵌套复制项目无法显示，以及单集故事被少量 GPT-SoVITS 碎句阻断的问题。
- `v0.2.8 / 仓库 2.0.8`：完善真实配音时长校准、手动分集、三分钟核心压缩、统一开关样式，以及 1080×1920 Shorts 竖屏输出与最终画布字幕预览；纯文稿功能保持开发预览状态。
- `v0.2.7`：完成多信号故事保真判断与自动系列拆分；每集继续复用镜头、配音、字幕、检查与导出流程。
- `v0.2.6`：分层理解改为自动默认；文案审查应用状态持久化。
- `v0.2.5`：统一事实、证据和术语文案审查；增加黑画面、静音和异常音量深度检查。
- `v0.2.3`：增加真实配音超时智能修正、精简前归档、双栏比较和恢复。
- `v0.2.0`：完成单应用整合，迁移 V1 的字幕、同步 SRT、配音变速与成片检查能力并删除 V1。
- `v0.1.8`：Git 快进更新与 ZIP 清单同步混合更新。
- `v0.1.7`：应用内更新升级为仓库级同步。
- `v0.1.6`：增加图形化 API 地址、密钥和模型管理。
- `v0.1.5`：Faster-Whisper 模型自动下载及下载进度。
- `v0.1.4`：启动时静默检查更新及无 CMD 启动器。
- `v0.1.3`：统一四步首页、最近项目管理、GPT-SoVITS 断句和 SRT 导出。
- `v0.1.1`～`v0.1.2`：API 前置检查、仅本地预处理和图形化快速设置。

## 整体待办与进度

| 计划 | 当前状态 | 说明 |
|---|---|---|
| 文案审查（事实、证据与术语一致性） | 已完成 | 可选功能，不阻断流程；应用建议后重算时长、重建 SRT 和镜头匹配。 |
| 分层理解长视频 | 已完成并默认自动 | 短片省略冗余调用，长片分片后综合；失败自动回退。 |
| 画面文字、图表和公式理解 | 已完成 | 合并到关键帧视觉理解请求；重要技术画面按需高清复查。 |
| 成片质量检查 | 已完成 | 包含项目、时间线、字幕、编码、黑场、静音、音量和真实输出验证。 |
| 自动叙事策略 | 已完成 | 默认自动判断，也可手动选择；不要求用户按题材挑选素材。 |
| 真实配音超时智能修正 | 功能完成，真实闭环待继续验证 | 支持安全加速、AI 精简、重匹配、版本比较和恢复。 |
| 科普术语一致性 | StoryCut 范围已完成 | 检查术语、单位、物种名、机械名称和数字；具体读音仍由 GPT-SoVITS 处理。 |
| 迁移 V1 优质功能并删除 V1 | 已完成 | 旧项目优质能力已迁移，V1 已清理。 |
| 长片故事保真与自动拆分 | 第一版已完成 | 使用多信号判断是否需要拆分；暂定档，后续用更多真实高密度视频验证。 |
| 从纯文稿生成视频 | **开发预览** | 已完成文稿与分镜、素材入库与分析、全局自动匹配、候选原素材预览、置信提示和锁定，并已接通多来源视频/图片、配音、字幕、竖屏画布、成片检查与导出；下一步使用真实项目校准。 |

### 从文稿开始

版本策略：不再规划 `3.0.0`。StoryCut 继续按 `2.1.1`、`2.1.2`、`2.1.3` 等版本小步递增；纯文稿闭环完成后移除开发预览标记，不跳升大版本。视频流程与纯文稿流程继续在同一版本序列维护。

创建项目时增加“从视频开始”和“从文稿开始”两个入口。文稿项目保持四步：

1. **整理文稿**：粘贴或导入文稿，整理英文解说、目标时长和可选事实审查。
2. **规划分镜**：为每个旁白单元生成镜头用途、理想画面、必须内容、可接受替代、避用内容和搜索关键词；显示预计镜头段数、建议素材文件数和覆盖率。
3. **匹配素材**：支持本地素材文件夹、场景检测、缩略图、标签、全局自动匹配、镜头锁定和绿/黄/红置信状态；找不到准确素材时提示应补充什么画面。
4. **配音与导出**：复用 GPT-SoVITS、同步 SRT、字幕样式、成片检查和导出；真实配音导入后按实际时间再次调整镜头。

实施顺序：

1. 纯文稿项目、文稿导入与编辑、分镜列表、素材数量提示已经完成。
2. 本地素材导入，以及 Pexels、Pixabay 正式 API 搜索、候选预览、来源记录和确认下载已经完成。
3. 已完成本地与下载素材的增量场景分析、缩略图、保守基础标签、全局非重复匹配、替代候选、缺失提示和锁定恢复。
4. 已完成多来源视频与静态图片时间线合成，并复用配音、同步 SRT、字幕样式、竖屏画布、成片检查和根 `export/` 导出。
5. 候选素材可双击调用系统默认播放器或看图程序打开原文件；下一步用真实纯文稿项目校准场景阈值、匹配置信度和替换效率，后续再按需增加低置信度镜头 AI 复核，以及波形、地图、图表和知识卡等本地图形模板。

### 长期增强

- 直接内嵌 GPT-SoVITS
- 批量处理多个视频
- 竖屏智能主体跟踪（基础竖屏完整画面与居中裁切已完成）
- 自动生成标题、简介和标签
- 根据发布后的播放数据优化提示词
- 素材版权与来源管理
- 多语言成片

> 维护约定：功能说明、版本更新和整体路线图维护在根目录 `README.md`；当前开发状态维护在 `CODEX_HANDOFF.md`；阶段细节维护在 `docs/phases/`。上级目录 `todo.md` 已停用，历史开发交接位于 `docs/archive/`。

## 当前版本

- 仓库版本：`2.1.2`
- StoryCut 版本：`2.1.2`
